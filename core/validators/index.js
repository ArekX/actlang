const any = (...matchers) => params => {
    for (let param of params) {

    }
};

any(
    group(name('name', literal('value')), type(['param', 'preprocessor']))
)

'(","@name:"comma",min:0 param|preprocessor)@min:1,max:*'
'(("and" "wait" "on")@name:"and wait on" number "done")@min:0, max:1'

module.exports = {

};